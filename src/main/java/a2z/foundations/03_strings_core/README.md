# Strings Core

## Purpose
- Explain `String`, `StringBuilder`, immutability, and correct equality semantics.

## Next steps
1. Call out `.equals` vs `==`, builder usage, and thread-safety notes.
2. Add runnable snippets and tests that compare performance of different string builders.
3. Keep explanations concise since the DSA strings topic will expand further.
