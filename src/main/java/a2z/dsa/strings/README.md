# Strings

## Purpose
- Capture two-pointer, frequency map, parsing, and string-algorithms practice.

## Next steps
1. Use the topic template to record invariants (window size, character counts, parsing state).
2. Populate `problems.md` with canonical string problems (include difficulty tiers).
3. Add solutions/tests addressing null/empty strings, encoding, and pointer bounds.
