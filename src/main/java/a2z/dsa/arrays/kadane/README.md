# Arrays

## Purpose
- Home for array-centric canonical problems: prefix sums, Kadane, two pointers, cyclic sort, and subarray counting.
- Track patterns → problem entries → solutions/tests for each technique.

## Next steps
1. Copy `docs/templates/topic-readme.md` into this folder and customize patterns.
2. Populate `problems.md` using `docs/templates/problem-entry.md`.
3. Add `solutions/` samples with matching tests in `src/test/java/a2z/dsa/arrays/`.

## Current work-in-progress
- ✅ `Best Time to Buy and Sell Stock` solution lives in `solutions/`, practicing single-pass min tracking.
- ✅ A corresponding `BestTimeToBuyAndSellStockTest` exists under `src/test/java/a2z/dsa/arrays/`.
TEST
