# Sliding Window

## Purpose
- Focus on fixed-size, variable-size, at-most-K, and deque max/min sliding window templates.

## Next steps
1. Start from the topic template to capture invariants and critical window invariants.
2. Populate `problems.md` with canonical sliding window challenges.
3. Implement solutions/tests that verify pointer movement, window resizing, and boundary cases.
