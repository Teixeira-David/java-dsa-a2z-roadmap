# BST

## Purpose
- Collect canonical binary-search-tree problems (construction, validation, traversal, search-area narrowing, subtree sums).
- Emphasize invariants such as left < node < right, null guards, and recursion/iterative stack alternatives.
- Surface traversal order, balancing, and tree-state tracking that show up in medium/hard LeetCode patterns.

## Next steps
1. Seed current status from `docs/templates/topic-readme.md`.
2. Wire up `problems.md` with tiered BST problem checklists.
3. Add representative solutions plus matching tests under `solutions/` and `src/test/java/a2z/dsa/bst/`.

## Current work-in-progress
- Awaiting canonical problem entries in `problems.md`.
