# DSA Topics

This directory mirrors the roadmap taxonomy (basics → trie). Each topic folder must eventually include:
- `README.md` with “done means” checkpoints (use `docs/templates/topic-readme.md`)
- `problems.md` with canonical LeetCode entries (use `docs/templates/problem-entry.md`)
- `solutions/` plus matching tests under `src/test/java/a2z/dsa/<topic>/`

Current state: placeholders only. Use this README as a launchpad before adding problem content.
