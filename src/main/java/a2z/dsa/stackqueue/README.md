# Stack & Queue

## Purpose
- House basic stack/queue uses plus monotonic stack/queue and expression evaluation patterns.

## Next steps
1. Plug in the topic template and note the key invariants (monotonicity, guard cases).
2. Draft `problems.md` with canonical stack/queue problems.
3. Include solutions/tests that stress edge cases (empty stack, null head, negative inputs).
